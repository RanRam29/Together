# 🧭 ביקורת יישור + תוכנית המשך — 2026-07-13

> מסמך ארכיטקט. נבדק מול קוד חי (מיגרציות, פונקציות ענן, אפליקציה) — לא מול דיווחי סטטוס.
> קהל יעד: Antigravity (צד שרת) ו-Cursor (אפליקציה). סדר הסעיפים = סדר הביצוע.

---

## חלק א' — מה אומת כתקין (לא לגעת)

| נושא | היכן אומת |
|------|-----------|
| הקשחת הרשאות C1/C1a/C2/H1/M1/M2 — חסימת רישום עם תפקיד מיוחס, מעבר ל-`is_admin()`, הקפאת שדות אימות, `search_path` | `20260713000000_security_hardening_rls_auth.sql` — כולל התיקון הנכון של טריגרים כ-SECURITY INVOKER |
| D14 דירוג עיוור נאכף ברמת ה-RLS + הוסרה המדיניות העוקפת `reviews_select` | `20260712230000` + `20260713010000` |
| D30 ביטול בינה מלאכותית חיצונית — אפס קריאות חיצוניות, טקסט דטרמיניסטי | `calculate-matches/index.ts`, `process-daily-log/index.ts` |
| D27/D28 מחיקה דו-שכבתית כולל מחיקת קבצים פיזית מה-bucket | `anonymize_user` בגרסת `20260713000000` |
| D47 השעיה ⇦ הקפאת חיבורים פעילים + הודעה לצד השני | `admin_suspend_user` ב-`20260712230000` (+נוסח מעודן ב-v2) |
| D31 צד שרת — הורה שני: טבלת הזמנות, טריגר אי-שינוי, אימות טלפון מול `auth.users` | `20260712210000` + הקשחת M2 |
| אימות מפקח דרך `app_metadata.is_supervisor` (מקביל לאדמין) | `20260713010000` |
| פונקציות הענן מאובטחות: `send-sms` עם חתימת hook, `send-push`/`process-daily-log` במפתח סודי בלבד, `calculate-matches` עובר דרך RLS של המשתמש | `supabase/functions/*` |
| בידוד בין משתמשים: תצוגת TIER0 רק לפרסומים, בעלות על ילד ב-`get_matches_for_child`, מסמכים בתיקיית הבעלים בלבד, הזמנות לפי טלפון מאומת | נבדק רוחבית — לא נמצאה זליגה חדשה בין משתמשים |

---

## חלק ב' — פערים שנמצאו (לפי חומרה)

### 🔴 ב-1: כפתור "ייצוא נתונים" מומש במקום להימחק — הפרה ישירה של D25
- ההכרעה מ-2026-07-12 הייתה חד-משמעית: **הכפתור מוסר לגמרי, לא מתוקן.** בפועל מישהו הוסיף `handleExport`/`isExporting` ([config.tsx:122](apps/mobile/app/(staff)/config.tsx:122)) שמוריד לקובץ מקומי את תוצאת `export_system_data`.
- ה-RPC ‏`export_system_data` (‏`20260712150000_export_rpc.sql`) חי ופרוס: שופך במרוכז `profiles`, `children` (כולל צרכים), `daily_logs`, `checkins`, `analytics_events` — ללא סיבה מתועדת, ללא רישום ביומן ביקורת על התוכן, לקובץ לא מוצפן מחוץ למערכת. זה בדיוק מה ש-D25 נועדה למנוע.
- **תיקון (Antigravity):** מיגרציה חדשה עם `DROP FUNCTION public.export_system_data();` + בדיקת pgTAP שהפונקציה איננה.
- **תיקון (Cursor):** מחיקת כל בלוק הייצוא מ-`config.tsx` (השורות 74, 122–148, 236–249).

### 🟠 ב-2: D46 הושלמה רק חצי — ימי חופשה לא מוחרגים מהתראות חוסר-פעילות
- `match_days_off` ו-`mark_day_off` קיימות, אבל `get_live_ops_alerts` (‏`20260712170000`) עדיין סופר 3 ימים קלנדריים ללא צ'ק-אין. כל מטרת D46 הייתה שחופשה מדווחת לא תדליק התראת "חיבור לא פעיל".
- **תיקון (Antigravity):** עדכון ההתראה כך שהחלון של 3 הימים לא יכלול תאריכים שקיימת להם שורה ב-`match_days_off` לאותו חיבור.

### 🟠 ב-3: מדיניות המסמכים ב-Storage נשארה על הבדיקה החלשה
- "Allow admins to view all documents" (‏`20260707110812`) עדיין בודקת `get_user_role() = 'admin'` — בעוד שכל שאר המערכת עברה ל-`is_admin()` (הקשחת C1a). זה ה-bucket הרגיש ביותר (תעודות זהות, מסמכי רישום פלילי).
- בנוסף: אין למפקח מדיניות צפייה במסמכים למרות שתור האימות הוא כל תפקידו (D26) — לוודא שהצפייה שם עוברת דרך URL חתום שמונפק ב-RPC מוקשח, ואם לא — להוסיף מדיניות מפקח תחומה למסמכים של משלבות שמשויכות אליו בלבד.
- **תיקון (Antigravity):** מיגרציה שמחליפה ל-`is_admin()`, ומסדירה את נתיב הצפייה של המפקח.

### 🟡 ב-4: מיגרציית D46/D48 נכתבה בניגוד לתבנית ההקשחה שנקבעה באותו יום
ב-`20260713020000_d46_d48_features.sql` (רצה **אחרי** מיגרציות ההקשחה):
1. `hide_match_profile` ו-`mark_day_off` הן SECURITY DEFINER **בלי** `SET search_path = public, pg_temp` (הפרת תבנית M1).
2. `get_matches_for_child` נוצרה מחדש עם הבדיקה החלשה `get_user_role() = 'admin'` במקום `is_admin()`, וכך גם מדיניות הקריאה של `match_days_off`.
3. `mark_day_off` בלי ולידציה: אפשר לסמן תאריך עתידי/עבר רחוק, ואין בדיקה שהחיבור פעיל.
- **תיקון (Antigravity):** מיגרציית "הקשחה v3" קצרה שמיישרת את שלושת הסעיפים.

### 🟡 ב-5: D48 מסתיר לפי הורה, לא לפי ילד — הכרעה מוצרית נדרשת
- `match_hides` מפתחת על `hidden_user_id` (ההורה). משלבת שלוחצת "לא מתאים" על ילד אחד מסתירה גם את כל שאר ילדי אותו הורה. ייתכן שזה מקובל (החיכוך הוא בין אנשים) — אבל זו סטייה מהניסוח המקורי ("הפרופיל השני").
- **הכרעת ארכיטקט: נשאר כמות שהוא ל-MVP** (הסתרה ברמת אדם הגיונית מוצרית), אבל מתועד כאן כדי שלא ייחשב באג בעתיד.

### 🟢 ב-6: שאריות קטנות
1. **הלוח מציג "הצעד הבא: הגדרת `CLAUDE_API_KEY`"** — סותר את D30 (אין יותר קריאות חיצוניות). למחוק את הסעיף מהלוח; אין להגדיר את המפתח בכלל.
2. `anonymize_user` שלב 8 עדיין מסיים חיבורים בלי לקבוע `ended_at` — פוגע בנתיב "14 יום מסיום" של D14. לתקן באותה מיגרציית v3.
3. הנוסח בדיווח היומי "מלווה באתגרים" קרוב מדי לנוסח שנפסל ב-D30; לעדן לפי `06-COPY-TONE`.
4. קבצים לא מחויבים בעץ העבודה: מדריך שימוש (`components/guide/`, `guide-content.ts`, `guide-store.ts` — נבדקו, תקינים, ממשק בלבד) ותיקון `seed-test-login.sql`. **לקמט (commit) עכשיו** כדי שהסוכנים לא ידרסו זה את זה.

---

## חלק ג' — תוכנית המשך (סדר מחייב)

### שלב 1 — סגירת פערי הביקורת (מיידי, לפני כל פיצ'ר חדש)
| # | משימה | אחראי |
|---|-------|--------|
| 1.1 | מיגרציה: מחיקת `export_system_data` + עדכון `get_live_ops_alerts` (חופשות) + הקשחה v3 (ב-3, ב-4, ב-6.2) | Antigravity |
| 1.2 | בדיקות pgTAP חדשות: היעדר ה-RPC של הייצוא, `match_hides` (רואה רק את שלו), `mark_day_off` (רק משתתף), החרגת חופשות מהתראות | Antigravity |
| 1.3 | מחיקת בלוק הייצוא מ-`config.tsx` + קימוט קבצי המדריך | Cursor |
| 1.4 | עדכון לוח התיאום: הסרת סעיף `CLAUDE_API_KEY`, סימון סעיפי ביקורת שנסגרו | שני הסוכנים |

### שלב 2 — חוסמי השקה מוצריים
| # | משימה | אחראי |
|---|-------|--------|
| 2.1 | D31 — ממשק הורה שני מלא: הזמנה, קבלה, ניהול הרשאות, התראות אחרי-מעשה | Cursor (השרת מוכן) |
| 2.2 | D45 — מתגי "מה דנה רואה" פר-שדה פר-זוג: דורש טבלת הרשאות חדשה + פירוק `get_child_details` להחזרה לפי מתגים. **דורש מפרט ארכיטקט לפני מימוש — לא להתחיל לבד** | ארכיטקט ⇦ Antigravity ⇦ Cursor |
| 2.3 | D44 — חסימת צילום מסך בתיק הילד (אנדרואיד: חסימה; iOS: זיהוי + רישום ביומן) | Cursor |
| 2.4 | משפטי (לא קוד, חוסם ציבור): חוות דעת עו"ד לבדיקת עברייני מין (תשס"א) + נוסח הסכמה לפי תיקון 13 | בעל המוצר |

### שלב 3 — דוחות ונתונים בגבולות הפרטיות
העיקרון: **דוחות מצטברים בלבד, לעולם לא יצוא גולמי.** במקום הכפתור שנמחק:
| # | משימה |
|---|-------|
| 3.1 | RPC-ים מצטברים לאדמין (ללא פרטים אישיים): חיבורים פעילים לאורך זמן, זמן ממוצע לאימות משלבת, אחוז דיווחים יומיים, משפכי בקשות. כל RPC עם `is_admin()` + `check_admin_mfa()` |
| 3.2 | מסך דוחות באזור הניהול שמציג את המצטברים (גרפים, ללא שמות) |
| 3.3 | צפייה פרטנית חריגה — רק דרך מנגנון "צפייה מנומקת" של D25: סיבה חובה + רישום ביומן הביקורת |
| 3.4 | מדיניות שמירת נתונים (retention): כמה זמן שומרים `daily_logs`/`checkins`/`analytics_events` — להכריע ולתעד ב-01-DECISIONS |

### שלב 4 — קדם-השקה
| # | משימה |
|---|-------|
| 4.1 | הרשמת MFA-TOTP לאדמין בענן + בדיקה שכל RPC ניהולי אוכף אותה |
| 4.2 | בדיקת קצה-לקצה ידנית על מכשיר אמיתי (טלפון + אימייל + שחזור סיסמה + קישור עמוק) |
| 4.3 | הגבלת קצב (rate limiting) על OTP ועל `hide_match_profile`/`mark_day_off` |
| 4.4 | צמצום CORS בפונקציות הענן מ-`*` לדומיינים של האפליקציה |
| 4.5 | גיבויים: אימות שגיבוי הענן פעיל + תרגול שחזור |

---

*כל סימון "בוצע" בשלבים 1–2 מחייב: pgTAP ירוק מקומית, `tsc` נקי, ואימות מול ה-DoD ב-`product/09`. אין לסמן על סמך דיווח בלבד.*
